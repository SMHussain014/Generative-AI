syntax = "proto3";

enum OperationType {
  CREATE = 0;
  UPDATE = 1;
  DELETE = 2;
}

message Product {
  optional int32 id = 1;
  optional string name = 2;
  optional string description = 3;
  optional float price = 4;
  optional string expiry = 5;
  optional string brand = 6;
  optional float weight = 7;
  optional string category = 8;
  optional string sku = 9;
  optional OperationType operation = 10;
}
    